# ✅ АККАУНТ KANA (SKAI.MEDIA) СОЗДАН!

## 🎉 ДЕТАЛИ АККАУНТА:

```
ID:       1
Email:    kana.bahytzhan@gmail.com
Password: Kanaezz15!
Company:  skai.media
Status:   Active
```

---

## 🔑 ДОСТУП:

### 1. Веб-чат (для клиентов):
```
http://192.168.0.10:8000/
```

**Функционал:**
- Клиенты пишут текстом или голосом
- Все заявки автоматически привязываются к skai.media (owner_id=1)
- Guest Mode - без регистрации

### 2. Админ-панель:
```
http://localhost:8000/admin
Логин: admin
Пароль: admin123
```

**Функционал:**
- Просмотр всех заявок
- Изменение статусов
- Поиск и фильтрация
- Удаление тестовых

### 3. API (для мобильного приложения):

**Получить токен:**
```bash
curl -X POST http://localhost:8000/api/auth/login \
  -d "username=kana.bahytzhan@gmail.com" \
  -d "password=Kanaezz15!"
```

**Ответ:**
```json
{
  "access_token": "eyJhbGci...",
  "token_type": "bearer"
}
```

**Получить заявки:**
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
     http://localhost:8000/api/leads
```

**Обновить статус:**
```bash
curl -X PATCH \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status": "in_progress"}' \
  http://localhost:8000/api/leads/1
```

---

## 🎯 КАК ЭТО РАБОТАЕТ:

### Сценарий 1: Клиент на сайте

1. **Клиент** открывает `http://192.168.0.10:8000/`
2. **Нажимает** кнопку микрофона 🎙️
3. **Говорит:** "Хочу дом в Алматы, 150 квадратов"
4. **AI бот** задает вопросы, собирает данные
5. **Заявка создается** автоматически с `owner_id=1` (skai.media)

### Сценарий 2: Вы в админке

1. **Открываете** `http://localhost:8000/admin`
2. **Видите** новую заявку в таблице
3. **Меняете статус** на "В работе"
4. **Звоните клиенту** и закрываете сделку

### Сценарий 3: Мобильное приложение

1. **Приложение** запрашивает токен (POST /api/auth/login)
2. **Получает список** заявок (GET /api/leads)
3. **Отображает** заявки в интерфейсе
4. **Обновляет статусы** (PATCH /api/leads/{id})

---

## 📊 ВАЖНО:

### Guest Mode:
- Все заявки с веб-чата автоматически привязываются к вашему аккаунту (ID=1)
- Клиентам НЕ нужно регистрироваться
- Вы видите все заявки в админке и через API

### Multi-tenancy:
- В будущем можно добавить других пользователей
- Каждый будет видеть ТОЛЬКО свои заявки
- Данные изолированы через `owner_id`

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ:

### 1. Протестируйте веб-чат:
```
http://192.168.0.10:8000/
```

- Нажмите микрофон
- Скажите "Хочу дом"
- Проверьте админку

### 2. Проверьте админку:
```
http://localhost:8000/admin
(admin / admin123)
```

- Откройте раздел "Заявки"
- Убедитесь что заявка появилась
- Измените статус

### 3. Получите JWT токен:
```bash
python test_kana_login.py
```

Скопируйте токен для мобильного приложения.

---

## 🔐 БЕЗОПАСНОСТЬ:

### Текущие пароли (DEV):

**Админ-панель:**
```
admin / admin123
```

**API:**
```
kana.bahytzhan@gmail.com / Kanaezz15!
```

### Для продакшена:

1. Смените пароль админки
2. Используйте HTTPS
3. Добавьте rate limiting
4. Смените SECRET_KEY

---

## 📱 ИНТЕГРАЦИЯ С МОБИЛЬНЫМ ПРИЛОЖЕНИЕМ:

### Flutter / React Native:

**1. Логин:**
```dart
// Flutter
final response = await http.post(
  Uri.parse('http://YOUR_SERVER/api/auth/login'),
  body: {
    'username': 'kana.bahytzhan@gmail.com',
    'password': 'Kanaezz15!'
  }
);

final token = jsonDecode(response.body)['access_token'];
```

**2. Получить заявки:**
```dart
final response = await http.get(
  Uri.parse('http://YOUR_SERVER/api/leads'),
  headers: {
    'Authorization': 'Bearer $token'
  }
);

final leads = jsonDecode(response.body);
```

**3. Обновить статус:**
```dart
await http.patch(
  Uri.parse('http://YOUR_SERVER/api/leads/$id'),
  headers: {
    'Authorization': 'Bearer $token',
    'Content-Type': 'application/json'
  },
  body: jsonEncode({'status': 'in_progress'})
);
```

---

## ✅ ВСЁ ГОТОВО!

**Ваш аккаунт создан и полностью функционален!**

- ✅ ID=1 (первый пользователь)
- ✅ Все заявки с веб-чата привязаны к вам
- ✅ Админка работает
- ✅ API доступен
- ✅ JWT токен генерируется

**Протестируйте веб-чат:**
```
http://192.168.0.10:8000/
```

**Проверьте заявки в админке:**
```
http://localhost:8000/admin
```

**🎉 ГОТОВО К ИСПОЛЬЗОВАНИЮ!**
