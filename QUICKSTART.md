# 🚀 Быстрый старт

## Установка за 3 шага

### 1️⃣ Создайте виртуальное окружение

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

Если возникает ошибка прав:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 2️⃣ Установите зависимости

```powershell
pip install -r requirements.txt
```

### 3️⃣ Запустите сервер

**Вариант 1 - Напрямую:**
```powershell
python main.py
```

**Вариант 2 - Через скрипт:**
```powershell
.\run.ps1
```

Или двойной клик на `run.bat`

---

## ✅ Проверка работы

### Откройте браузер:

**Главная страница:**
http://localhost:8000

**API документация (Swagger):**
http://localhost:8000/docs

**Health check:**
http://localhost:8000/api/health

---

## 🧪 Первый тест

### Через Swagger UI:

1. Откройте http://localhost:8000/docs
2. Найдите `POST /api/chat`
3. Нажмите "Try it out"
4. Введите:
   - `user_id`: `test_user_1`
   - `message_text`: `Здравствуйте, хочу построить дом`
5. Нажмите "Execute"

Бот должен ответить на русском языке и начать квалификацию.

### Через Python скрипт:

```powershell
python test_requests.py
```

---

## 📱 Telegram уведомления

Чтобы получать уведомления о лидах:

1. Найдите вашего бота в Telegram (токен из `.env`)
2. Напишите боту `/start`
3. Пройдите полный диалог в API с контактами
4. Лид появится в Telegram с кнопками управления

---

## 🆘 Частые проблемы

### Ошибка "ModuleNotFoundError"
```powershell
pip install -r requirements.txt --upgrade
```

### Telegram бот не отвечает
Проверьте `TELEGRAM_BOT_TOKEN` в `.env`

### OpenAI ошибка авторизации
Проверьте `OPENAI_API_KEY` в `.env`

### База данных не создается
Убедитесь, что есть права на запись в текущей директории

---

## 📚 Полная документация

Смотрите [README.md](README.md) для подробной информации.

---

**Готово! Теперь у вас работает ИИ-Менеджер по продажам 🎉**
